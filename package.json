{
  "name": "yinkaiyu-github-io",
  "private": true,
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "playwright:test": "playwright test",
    "playwright:codegen": "playwright codegen http://127.0.0.1:8000"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
